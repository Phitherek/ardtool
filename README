ARDTool - A simple Automatic Rails Deployment Tool
==============================

This project' s purpose is to provide a simple helper tool for automatic, one-command deployment of Rails applications. It is tested with RVM to ensure no conflicts will occur. It is written in C++ and is still in heavy development. Check back for more!
